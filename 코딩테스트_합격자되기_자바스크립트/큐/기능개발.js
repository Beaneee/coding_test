const solution = (progresses, speeds) => {
  // 각 progress마다 개발이 언제 완료되는지 알아야한다.
  // (100 - progress) / speeds[idx]

  // 100을 초과하는 시점이 개발이 완료됐다는 의미이다.
  // 개발이 완료된 front부터 차례대로 뽑는다.

  // 뽑힌 수를 result에 push한다.

  // result를 return

}