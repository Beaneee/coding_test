// 전위
const preorder = (nodes, idx) => {
  // 부모 -> 왼쪽 -> 오른쪽
}

// 중위
const indorder = (nodes, idx) => {
  // 왼쪽 -> 부모 -> 오른쪽
}

// 후위
const postorder = (nodes, idx) => {
  // 왼쪽 -> 오른쪽 -> 부모
}


const solution = (nodes) => {
  // 전위
  // 중위
  // 후위
}

console.log(solution([1,2,3,4,5,6,7]));