const solution = (graph, start) => {
  // 인접 리스트로 만들고

  // dfs 함수를 작성한다.

  // 순회한 값을 return
}

solution([['A', 'B'], ['B', 'C'], ['C', 'D'], ['D', 'E']], 'A');
solution([['A', 'B'], ['A', 'C'], ['B', 'D'], ['B', 'E'], ['C', 'F'], ['E', 'F']], 'A');
