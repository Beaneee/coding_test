const dfs = (graph, node, visited) => {
  const stack = [];
  // 시작 노드를 push하고
  // 방문처리

  // stack이 비어있다면 종료

  // pop
  // 방문하지 않았다면 방문처리

  // 인접노드를 확인해서
  // 방문하지 않은 노드들에 대해서만
  // 재귀
}

function solution(n, computers) {
  // 인접 리스트를 만들고
  // dfs를 진행한다.
}