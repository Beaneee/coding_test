const solution = (s) => {
  console.log(s);

  // s의 길이 만큼 pop / push를 진행한다.

  // 괄호 짝이 맞는지 검사한다.

  // 맞으면 result를 하나씩 더해준다.

  return 0;
}



console.log(solution('[](){}'));
console.log(solution('}]()[{'));
console.log(solution('[)(]'));
console.log(solution('}}}'));
