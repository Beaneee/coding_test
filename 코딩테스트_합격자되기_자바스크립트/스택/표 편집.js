const solution = (n, k, cmd) => {
  // 길이가 n인 배열 생성

  // cmd 명령대로 수행할 함수

  // switch
  // U X -> k + x;
  // D X -> k - x;
  // C -> split k, push stack;
  // Z -> pop stack, pop된 value를 다시 배열에 삽입;

  // stack에 있는 key의 value만 O로 표시해서 return
}
