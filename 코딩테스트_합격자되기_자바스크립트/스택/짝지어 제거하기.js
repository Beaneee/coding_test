const solution = (s) => {

  // 문자열을 순회하면서

  // 문자를 하나 뽑아서 다음 문자열과 같은지 비교한다.

  // 같다면 2개의 문자를 pop

  // 아니라면 유지한다.

  // 문자열이 ''이면 1 아니면 0을 return

}

console.log(solution('baabaa'));
console.log(solution('cdcd'));